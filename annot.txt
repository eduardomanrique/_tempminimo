erro quando carrega script que nao existe (ver o onerror)
erro no cache com recursos que nao existem. Tem que validar se o recurso existe antes de colocar no cache
appcache com htmx e .js de tela


X.import, X.modalS retorna promise
mutation 

import
    implementar require do node?
    importando outro recurso js no padrao minimo (em vez do require) o recurso so pode ser dado como iniciado se ele chamou seu onInit() e o mesmo terminou.
eliminar textos vazios da compilacao final do struct
ver BrowserElements being created withoun struct
modal

GenericBrowserElement retirar

show hide